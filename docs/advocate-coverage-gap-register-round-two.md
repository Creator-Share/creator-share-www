# Advocate Coverage Gap Register, Round Two

Twenty-two further places where a deliberate, compiling change to production TypeScript passes the entire required suite.

## Read this first

**Every entry is a missing assertion, not a live defect.** The committed code is correct. Each row records that if someone broke that specific behaviour, nothing would fail.

This is a second sweep, over eight subsystems the first one never touched: sponsor accounts, retention workers, audit, storage and media, email workers, administrator lifecycle controls, cancellation and payment-method management, and the sponsor authentication routes. The first sweep's register is closed; see `docs/advocate-coverage-gap-register.md`.

## Method, and the one column that matters

Each proposal was mechanically applied and the suite re-run. All twenty-two survived. That number alone means little, so every file was then probed by appending a throwing statement and running the complete offline lane again:

- **assertion gap** — the suite loads this file, so a surviving mutation is a genuine, specific missing assertion. **Fifteen rows.**
- **no test loads the file** — nothing imports it in any lane, so the finding is about the whole file rather than the line. **Seven rows.**

Two predictions I made before the probe were wrong, which is why the column exists rather than an assumption: the administrator lifecycle and ownership-transfer components _are_ loaded, and the two email runtime modules are _not_.

## The register

| #   | Severity    | Kind                   | File                                                                       | What would break undetected                                                                                                                                                                                                                                                                                                                 |
| --- | ----------- | ---------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | security    | assertion gap          | `src/app/api/auth/verify-otp/route.ts`                                     | `isVerifiedRecoveryUser` stops rejecting an actively banned/suspended provider account. A sponsor whose Supabase auth user carries a future `banned_until` can complete recovery OTP verificat **CLOSED** by `tests/auth/verify-otp.spec.ts`                                                                                                |
| 2   | security    | assertion gap          | `src/app/api/internal/sponsorships/subscription-cancellations/route.ts`    | Any anonymous GET or POST to /api/internal/sponsorships/subscription-cancellations that simply omits the Authorization header now runs a full cancellation worker batch. The bearer-secret gat **CLOSED** by `tests/sponsorships/subscription-cancellation-worker-route.spec.ts`                                                            |
| 3   | security    | assertion gap          | `src/lib/retention/dataRetentionConfig.ts`                                 | The data-retention worker endpoint (/api/internal/retention, service-role client, drives every erasure/redaction RPC) will now boot with a one-character CRON_SECRET instead of failing closed **CLOSED** by `tests/advocates/data-retention-worker.spec.ts`                                                                                |
| 4   | security    | assertion gap          | `src/lib/retention/dataRetentionConfig.ts`                                 | The only minimum-entropy floor on the bearer secret that authorizes the data-retention worker drops from 32 characters to 4. An operator who sets CRON_SECRET (or DATA_RETENTION_WORKER_SECRET **CLOSED** by `tests/advocates/data-retention-worker.spec.ts`, the same guard as the row above and verified to fail under this threshold too |
| 5   | security    | assertion gap          | `src/lib/sponsorships/email/sponsorWelcomeEmailConfig.ts`                  | Removes the 32-character entropy floor on the sponsor welcome worker's bearer secret. Unlike the invitation worker, isAuthorizedSponsorWelcomeEmailWorkerRequest in sponsorWelcomeEmailAuth.ts **CLOSED** by `tests/sponsorships/sponsor-welcome-email-worker.spec.ts`                                                                      |
| 6   | security    | assertion gap          | `src/utils/auth/requireSuperAdmin.ts`                                      | Any authenticated user who holds any role assignment at all (an advocate portal member, a tenant editor, any non-super-admin role) passes the super-admin gate. That gate is the only authoriz **CLOSED** by `tests/auth/require-super-admin.spec.ts`                                                                                       |
| 7   | security    | assertion gap          | `src/utils/passwordValidation.ts`                                          | The platform-wide minimum password length collapses from 8 to 4. `validatePassword` is the single gate used by the password-change route (src/app/api/auth/change-password/route.ts:114), the **CLOSED** by `tests/auth/password-validation.spec.ts`                                                                                        |
| 8   | money       | assertion gap          | `src/lib/sponsorships/cancellation/subscriptionCancellation.ts`            | cancelStripeSubscription reports provider_cancelled for any Stripe subscription that comes back in a state other than "incomplete" — including "active", "past_due", and "trialing". A cancel **CLOSED** by `tests/sponsorships/subscription-cancellation-provider.spec.ts`                                                                 |
| 9   | money       | assertion gap          | `src/lib/sponsorships/cancellation/subscriptionCancellation.ts`            | The Stripe branch no longer verifies that the claimed provider object is actually a subscription. Any claim with a Stripe scope but a different provider_object_type (invoice, payment_intent, **CLOSED** by `tests/sponsorships/subscription-cancellation-provider.spec.ts`                                                                |
| 10  | privacy     | assertion gap          | `src/app/api/beneficiaries/[id]/activities/route.ts`                       | DOCUMENT media attached to an activity (PDFs such as school reports, medical notes, or signed paperwork for a child) are folded into the public `images_url` array. The unauthenticated public **CLOSED** by `tests/advocates/public-beneficiary-detail-host-routing.spec.ts`                                                               |
| 11  | privacy     | assertion gap          | `src/lib/advocates/logoReconciliation/route.ts`                            | The logo-reconciliation cron (runs every minute, deletes advocate logo objects from the advocate-assets bucket) stops treating permanently failed deletions as an incident. When jobs exhaust **CLOSED** by `tests/advocates/logo-reconciliation-worker.spec.ts`                                                                            |
| 12  | privacy     | assertion gap          | `src/lib/retention/dataRetentionRoute.ts`                                  | Removes the sanitization on the trace header that the retention route forwards into Postgres. Whatever an upstream caller puts in x-vercel-id / cf-ray / traceparent / x-trace-id is now passe **CLOSED** by `tests/advocates/data-retention-worker.spec.ts`                                                                                |
| 13  | privacy     | assertion gap          | `src/utils/supabase/buckets.ts`                                            | Every beneficiary/activity image and video URL is composed against a bucket that does not exist, so `getDirectMediaUrl`, `getExternalImageUrl`, and `getPublicUrlFromKey` emit 404 URLs across **CLOSED** by `tests/media-urls.spec.ts`                                                                                                     |
| 14  | correctness | assertion gap          | `src/components/advocates/creatorShareAdmin/AdvocateLifecycleControls.tsx` | The typed confirmation gate on the irreversible "Archive portal" action stops requiring the exact portal-bound phrase `ARCHIVE <slug>` and enables the submit button on any non-blank input. A **CLOSED** by `tests/advocates/creator-share-advocate-control-rerender.spec.ts`                                                              |
| 15  | correctness | assertion gap          | `src/components/advocates/creatorShareAdmin/AdvocateOwnershipTransfer.tsx` | The sole-owner-change confirmation stops being bound to the portal being viewed: any input beginning with "TRANSFER" (including the confirmation phrase for a different advocate portal, e.g. **CLOSED** by `tests/advocates/creator-share-advocate-control-rerender.spec.ts`                                                               |
| 16  | security    | no test loads the file | `src/app/(admin)/admin/advocates/[id]/page.tsx`                            | Ownership transfer must use only server-computed eligible memberships. **CLOSED** by `tests/advocates/creator-share-advocate-lifecycle.spec.ts`, with the page wired to the tested selector.                                                                                                                                                 |
| 17  | security    | no test loads the file | `src/app/(advocate-admin)/portal/[slug]/team/page.tsx`                     | Read-only team viewers must not receive management or invitation capabilities. **CLOSED** by `tests/advocates/portal-access.spec.ts`, with the page wired to the tested capability derivation.                                                                                                                                              |
| 18  | money       | no test loads the file | `src/app/(app)/app/components/OneTimeSponsorshipHistory.tsx`               | Net charged amount must remain primary after a refund, reversal, or withholding adjustment. **CLOSED** by `tests/sponsorships/sponsor-account-history.spec.ts`, with the component wired to the tested money presentation.                                                                                                                    |
| 19  | privacy     | no test loads the file | `src/lib/advocates/invitations/emailRuntime.ts`                            | canaryContract is typed as the single literal "creator-share-ff029-v1" and loadAdvocateStagingEmailRecipientPolicy is the only producer, so the added disjunct is always true and assertRecipi **CLOSED** by `tests/advocates/invitation-email-runtime.spec.ts`                                                                             |
| 20  | correctness | no test loads the file | `src/app/(app)/app/page.tsx`                                               | Incomplete or cancelled subscriptions must not inflate active, blind, or partnership counts. **CLOSED** by `tests/sponsorships/sponsor-recurring-sponsorships.spec.ts`, with the page wired to the tested summary.                                                                                                                           |
| 21  | correctness | no test loads the file | `src/app/api/sponsor-account/history/route.ts`                             | The RPC is asked for exactly pageSize rows instead of the pageSize+1 lookahead that buildSponsorAccountHistoryPage uses to detect a next page. `parsed.length > pageSize` can then never be tr **CLOSED** by `tests/sponsorships/sponsor-account-history-route.spec.ts`                                                                     |
| 22  | correctness | no test loads the file | `src/lib/sponsorships/email/sponsorWelcomeEmailRuntime.ts`                 | processSponsorWelcomeEmail falls back to `dependencies.invocationDeadlineAt ?? Infinity`, so the invocation-deadline half of assertSendHeadroom can never fire in production. The welcome-emai **CLOSED** by `tests/sponsorships/sponsor-welcome-email-runtime.spec.ts`                                                                     |

## Where to start

Two rows have the worst failure modes and should be closed first.

`src/lib/retention/dataRetentionConfig.ts` enforces a 32 character floor on the retention worker secret. Lowering it to 1 is accepted, so a truncated or misconfigured environment variable turns a service-role endpoint that drives every erasure and redaction into something an unauthenticated caller can reach by guessing a one-character bearer. The floor is the only thing standing between a bad env var and an open endpoint.

`src/components/advocates/creatorShareAdmin/AdvocateOwnershipTransfer.tsx` filters ownership-transfer targets on server-computed eligibility. Widening it to "not the current owner" offers suspended and revoked memberships as valid targets for handing over a live portal, and directly contradicts the copy rendered above the form.

Also worth naming: `src/app/api/beneficiaries/[id]/activities/route.ts` already has a test, which asserts _which tenant's_ data it serves. It asserts nothing about _what it projects_, so folding DOCUMENT media into the public images array is undetected. Covering a route's host scoping is not the same as covering its payload.

## Final component rows

The last four rows are closed without a second Chakra runtime. Each page now delegates its critical decision to a pure, typed function. The tests exercise those functions with adversarial inputs and assert that each page or component consumes the tested seam.

This is narrower and more reliable than an isolated fixture that constructs a different Chakra provider graph. The failed fixture experiment remains useful evidence: `ChakraProvider value={defaultSystem}` hit `datePickerAnatomy.extendWith is not a function` during configuration collection because the fixture resolved a different Ark and Chakra graph. Adding `experimental.optimizePackageImports` did not change the failure. No release contract depends on that broken fixture path.
